version: "1.0"

imports:
    # see config/deploy-templates
    deploy.live.template.yml:
        parameters:
            namespace: 'app-stripe'
            environment: 'stripe.spryker.local'

            database.name: 'my-app-docker'
            database.user: 'spryker'
            database.password: 'secret'
            database.root_password: 'secret'

            hosts.glue: 'my-app.spryker.local'
